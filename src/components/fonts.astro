---
// Font variables for Astro - fonts are loaded through CSS
export const fontMono = {
  variable: '--font-mono',
  className: 'font-mono'
};

export const fontSans = {
  variable: '--font-sans',
  className: 'font-sans'
};
---

<!-- This component is used for font configuration and doesn't render anything -->
